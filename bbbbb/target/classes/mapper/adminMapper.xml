<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">
	<insert id="addAdmin" parameterType="admin.dto.AdminDTO">
		INSERT INTO admin_data(admin_id,
		admin_password, admin_Name)
		VALUES (#{admin_id,jdbcType=VARCHAR},
		#{admin_password,jdbcType=VARCHAR}, #{admin_name,jdbcType=VARCHAR})
	</insert>
	<!-- 아이디 값으로 사용자 선택 -->
	<select id="selectByadmin_id" parameterType="string"
		resultType="admin.dto.AdminDTO">
		SELECT *
		FROM admin_data
		WHERE admin_id = #{loginId,jdbcType=VARCHAR} AND admin_password=#{loginPw , jdbcType=VARCHAR} 
	</select>
	<select id="idcheck" parameterType="string" resultType="int">
		SELECT
		COUNT(admin_id)
		FROM admin_data
		WHERE admin_id = #{admin_id,jdbcType=VARCHAR}
	</select>
	<select id="printAdmin" resultType="admin.dto.AdminDTO">
		SELECT *
		FROM admin_data
		
	</select>
		<select id="printUser" 
		resultType="admin.dto.AdminDTO">
		SELECT *
		FROM user_data
		
	</select>


</mapper>